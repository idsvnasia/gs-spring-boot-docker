apiVersion : apps/v1
kind: Deployment
metadata:
  name: "javaspringboot-2f40"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "javaspringboot-2f40"
  template:
    metadata:
      labels:
        app: "javaspringboot-2f40"
    spec:
      containers:
        - name: "javaspringboot-2f40"
          image: "idsvnasiaacr.azurecr.io/javaspringboot"
          ports:
          - containerPort: 8080