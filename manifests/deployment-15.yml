apiVersion : apps/v1
kind: Deployment
metadata:
  name: "javaspringboot-0773"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "javaspringboot-0773"
  template:
    metadata:
      labels:
        app: "javaspringboot-0773"
    spec:
      containers:
        - name: "javaspringboot-0773"
          image: "idsvnacr.azurecr.io/javaspringboot"
          ports:
          - containerPort: 80