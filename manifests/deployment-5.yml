apiVersion : apps/v1
kind: Deployment
metadata:
  name: "javaspringboot-cac3"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "javaspringboot-cac3"
  template:
    metadata:
      labels:
        app: "javaspringboot-cac3"
    spec:
      containers:
        - name: "javaspringboot-cac3"
          image: "idsvnasiaacr.azurecr.io/javaspringboot"
          ports:
          - containerPort: 8080