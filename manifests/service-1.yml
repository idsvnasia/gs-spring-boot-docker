apiVersion: v1
kind: Service
metadata:
    name: "javaspringboot-af06"
    labels:
        app: "javaspringboot-af06"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "javaspringboot-af06"