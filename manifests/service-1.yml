apiVersion: v1
kind: Service
metadata:
    name: "javaspringboot-b47b"
    labels:
        app: "javaspringboot-b47b"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "javaspringboot-b47b"