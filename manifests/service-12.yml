apiVersion: v1
kind: Service
metadata:
    name: "javaspringboot-eb5d"
    labels:
        app: "javaspringboot-eb5d"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "javaspringboot-eb5d"