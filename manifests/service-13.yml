apiVersion: v1
kind: Service
metadata:
    name: "javaspringboot-4a79"
    labels:
        app: "javaspringboot-4a79"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "javaspringboot-4a79"