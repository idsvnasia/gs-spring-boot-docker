apiVersion: v1
kind: Service
metadata:
    name: "javaspringboot-97e2"
    labels:
        app: "javaspringboot-97e2"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "javaspringboot-97e2"