apiVersion: v1
kind: Service
metadata:
    name: "javaspringboot-0e5c"
    labels:
        app: "javaspringboot-0e5c"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "javaspringboot-0e5c"