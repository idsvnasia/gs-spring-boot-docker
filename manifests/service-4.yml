apiVersion: v1
kind: Service
metadata:
    name: "javaspringboot-6b2d"
    labels:
        app: "javaspringboot-6b2d"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "javaspringboot-6b2d"