apiVersion: v1
kind: Service
metadata:
    name: "javaspringboot-cac3"
    labels:
        app: "javaspringboot-cac3"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "javaspringboot-cac3"