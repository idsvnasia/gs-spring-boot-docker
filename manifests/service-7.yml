apiVersion: v1
kind: Service
metadata:
    name: "javaspringboot-78bd"
    labels:
        app: "javaspringboot-78bd"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "javaspringboot-78bd"