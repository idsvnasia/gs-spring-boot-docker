apiVersion: v1
kind: Service
metadata:
    name: "javaspringboot-471e"
    labels:
        app: "javaspringboot-471e"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "javaspringboot-471e"