apiVersion: v1
kind: Service
metadata:
    name: "javaspringboot-v1"
    labels:
        app: "javaspringboot-v1"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "javaspringboot-v1"
